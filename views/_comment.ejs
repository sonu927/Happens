<li>
    <% if(locals.user && locals.user.id == c.user.id){%>
        <small>
            <a href="/comments/destroy/<%= c.id%>">X</a>
        </small>    
    <%} %>
    <p>
        <%= c.content%>
    </p>
    <small>
        <%= c.user.name%>
    </small>

</li>