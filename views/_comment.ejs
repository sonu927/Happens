<li id="comment-<%= c._id%>">
    <% if(locals.user && locals.user.id == c.user.id){%>
        <small>
            <a class="comment-delete-btn" href="/comments/destroy/<%= c.id%>">X</a>
        </small>    
    <%} %>
    <p>
        <%= c.content%>
    </p>
    <small>
        <%= c.user.name%>
    </small>
    <% if(locals.user){%>
        <a id="main-frame-like-<%= c.id%>" href="/likes/toggle/?id=<%= c._id%>&type=Comment&reaction=👍" data-likes="<%= c.likes.length%>" class="toggle-like-button">
            <% let like; %>
            <% for(l of c.likes){%>
                <% if(l.user == locals.user.id){%>
                    <% like = l; %>    
                <%} %>    
            <%} %>
            <%if(like){%>
                <%= c.likes.length%> <%= like.reaction%>    
            <%}else{%>
                <%= like%>
                <%= c.likes.length%> 👍
            <%}%>
        </a>
        <small>
            <ul>
                <li><a id="main-frame-like-<%= c.id%>" href="/likes/toggle/?id=<%= c._id%>&type=Comment&reaction=👍" data-likes="<%= c.likes.length%>" class="toggle-like-button">
                    <%= c.likes.length%> 👍
                </a></li>
                <li><a id="main-frame-like-<%= c.id%>" href="/likes/toggle/?id=<%= c._id%>&type=Comment&reaction=❤️" data-likes="<%= c.likes.length%>" class="toggle-like-button">
                    <%= c.likes.length%> ❤️
                </a></li>
                <li><a id="main-frame-like-<%= c.id%>" href="/likes/toggle/?id=<%= c._id%>&type=Comment&reaction=😂" data-likes="<%= c.likes.length%>" class="toggle-like-button">
                    <%= c.likes.length%> 😂
                </a></li>
                <li><a id="main-frame-like-<%= c.id%>" href="/likes/toggle/?id=<%= c._id%>&type=Comment&reaction=😲" data-likes="<%= c.likes.length%>" class="toggle-like-button">
                    <%= c.likes.length%> 😲
                </a></li>
                <li><a id="main-frame-like-<%= c.id%>" href="/likes/toggle/?id=<%= c._id%>&type=Comment&reaction=😥" data-likes="<%= c.likes.length%>" class="toggle-like-button">
                    <%= c.likes.length%> 😥
                </a></li>
                <li><a id="main-frame-like-<%= c.id%>" href="/likes/toggle/?id=<%= c._id%>&type=Comment&reaction=😡" data-likes="<%= c.likes.length%>" class="toggle-like-button">
                    <%= c.likes.length%> 😡
                </a></li>
            </ul>
        </small>
    <%}else{%>
        <p><%= c.likes.length%> Reaction</p>
    <%} %>
</li>